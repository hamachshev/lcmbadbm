package edu.touro.mco152.bm.command;

/**
 * Interface that represents the commands that can be passed into the command executor to be executed
 */
public interface BenchmarkCommand {
    public boolean execute();
}
